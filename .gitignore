# Build output
bin/
obj/
publish/
*.user

# Built installer artifacts (published to GitHub Releases, not committed)
installer/*.exe

# Rider / VS
.idea/
.vs/

# Local runtime data (the SQLite db lives in %APPDATA%\Treeline, but ignore any local copies)
*.db
*.db-shm
*.db-wal

# OS
Thumbs.db
.DS_Store
